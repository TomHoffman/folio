/**
 * When `false`, `status: "protected"` projects behave like active work in the app
 * (direct `/work/[slug]` links, no PIN shell). Re-enable when reworking the access flow.
 */
export const PROTECTED_WORK_GATE_ENABLED = false;
